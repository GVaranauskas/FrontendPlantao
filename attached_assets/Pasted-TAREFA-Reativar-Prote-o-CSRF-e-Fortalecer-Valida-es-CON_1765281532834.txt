TAREFA: Reativar Proteção CSRF e Fortalecer Validações

CONTEXTO:
CSRF está desabilitado (linhas 25-26 e 84-85 do server/index.ts). Isso deixa o sistema vulnerável a ataques.

EXECUTAR:

1. No arquivo server/index.ts:
   - Localizar linha 25-26 (comentário sobre CSRF)
   - DESCOMENTAR estas linhas:
```typescript
import { setupCSRF, csrfErrorHandler } from "./middleware/csrf";
setupCSRF(app);
```

   - Localizar linha 84-85 (comentário sobre CSRF error handler)
   - DESCOMENTAR:
```typescript
app.use(csrfErrorHandler);
```

2. Atualizar shared/schema.ts para fortalecer validação de senha:
   - Localizar insertUserSchema
   - Modificar validação de password para:
```typescript
password: z.string()
  .min(8, 'Senha deve ter no mínimo 8 caracteres')
  .regex(/[A-Z]/, 'Senha deve conter pelo menos uma letra maiúscula')
  .regex(/[a-z]/, 'Senha deve conter pelo menos uma letra minúscula')
  .regex(/[0-9]/, 'Senha deve conter pelo menos um número')
  .regex(/[^A-Za-z0-9]/, 'Senha deve conter pelo menos um caractere especial'),
```

3. Criar arquivo client/src/lib/csrf.ts:
```typescript
let csrfToken: string | null = null;

export async function fetchCsrfToken() {
  try {
    const response = await fetch('/api/csrf-token', {
      credentials: 'include',
    });
    const data = await response.json();
    csrfToken = data.csrfToken;
  } catch (error) {
    console.error('Failed to fetch CSRF token:', error);
  }
}

export function getCsrfToken(): string | null {
  return csrfToken;
}

export async function apiRequest(url: string, options: RequestInit = {}) {
  const headers = new Headers(options.headers);
  
  if (['POST', 'PUT', 'PATCH', 'DELETE'].includes(options.method || 'GET')) {
    if (!csrfToken) {
      await fetchCsrfToken();
    }
    if (csrfToken) {
      headers.set('X-CSRF-Token', csrfToken);
    }
  }
  
  return fetch(url, {
    ...options,
    headers,
    credentials: 'include',
  });
}
```

4. Atualizar client/src/main.tsx:
   - Adicionar no INÍCIO (antes do ReactDOM.render):
```typescript
import { fetchCsrfToken } from './lib/csrf';
fetchCsrfToken();
```

VALIDAÇÃO:
- CSRF descomentado no server/index.ts
- Validação de senha fortalecida
- Cliente busca token CSRF ao iniciar
- Código compila sem erros