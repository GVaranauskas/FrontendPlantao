# IMPORTANT: This file demonstrates required environment variables.
# NEVER commit actual values! Use Replit Secrets for sensitive data.
# See: https://docs.replit.com/tutorials/setting-up-replit-secret

# Database Configuration
# Get these from Replit's built-in PostgreSQL or create a Secrets entry
DATABASE_URL=postgresql://user:password@localhost:5432/nursing_platform
PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=your_secure_password
PGDATABASE=nursing_platform

# Session & Authentication Security
SESSION_SECRET=your_secure_session_secret_change_in_production_min_32_chars
NODE_ENV=development

# API Configuration
PORT=5000
VITE_API_URL=http://localhost:5000

# External Services
N8N_API_URL=https://n8n-dev.iamspe.sp.gov.br/webhook/evolucoes

# Security Settings (optional, defaults provided)
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100
CORS_ORIGIN=http://localhost:5000
